use crate::main;

#[test]
fn test_main() {
    // @dev - Noir Web Prover
    main(
        response_plaintext_bytes,
        line_digests,
        polynomial_input
        //1, 
        //2
    );

    // Uncomment to make test fail
    // main(1, 1);
}